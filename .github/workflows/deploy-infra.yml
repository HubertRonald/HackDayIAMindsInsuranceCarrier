<<<<<<< HEAD
name: Destroy Infra and App on GCP

on:
  workflow_dispatch:
=======
name: Deploy Infra and App to GCP

on:
  push:
    branches:
      - main
      - 'releases/**'
  pull_request:
    types: [opened, synchronize]
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da

env:
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  REGION: ${{ vars.GCP_REGION }}
  SERVICE_NAME: ${{ vars.GCP_SERVICE_NAME }}
<<<<<<< HEAD
=======
  BACKEND_DIR: ${{ vars.BACKEND_DIR }}
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da
  FRONTEND_DIR: ${{ vars.FRONTEND_DIR }}
  TF_DIR: ${{ vars.TF_DIR }}

jobs:
<<<<<<< HEAD
  terraform-destroy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

=======
  terraform:
    runs-on: ubuntu-latest
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da
    defaults:
      run:
        working-directory: ${{ env.TF_DIR }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

<<<<<<< HEAD
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'
=======
      - name: Setup GCP Key
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" > gcp-key.json
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

<<<<<<< HEAD
      - name: Terraform Init
        run: terraform init

      - name: Terraform Destroy
        run: terraform destroy -auto-approve -var="project_id=${{ env.PROJECT_ID }}"

  cleanup-cloud-run:
    needs: terraform-destroy
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Delete Cloud Run service
        run: |
          if gcloud run services describe $SERVICE_NAME --region $REGION > /dev/null 2>&1; then
            gcloud run services delete $SERVICE_NAME --region $REGION --quiet
          else
            echo "Cloud Run service $SERVICE_NAME no existe, saltando..."
          fi

      - name: Delete Artifact Registry images
        run: |
          gcloud artifacts docker images delete ${REGION}-docker.pkg.dev/$PROJECT_ID/hackday-repo/$SERVICE_NAME:latest \
            --quiet --delete-tags || echo "No image found to delete"

  cleanup-firebase:
    needs: terraform-destroy
    runs-on: ubuntu-latest
    steps:
=======
      - name: Init Terraform
        run: terraform init

      - name: Terraform Apply
        run: terraform apply -auto-approve \
          -var="project_id=${{ env.PROJECT_ID }}" \
          -var="credentials_file=$GITHUB_WORKSPACE/gcp-key.json"

  deploy-app:
    needs: terraform
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      # =============================
      # 1️⃣ BACKEND (Cloud Run)
      # =============================
      - name: Build and Push Backend Docker image
        run: |
          gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet
          gcloud builds submit $BACKEND_DIR \
            --tag ${REGION}-docker.pkg.dev/$PROJECT_ID/hackday-repo/$SERVICE_NAME:latest

      - name: Deploy Backend to Cloud Run (with SA auth)
        run: |
          gcloud run deploy $SERVICE_NAME \
            --image ${REGION}-docker.pkg.dev/$PROJECT_ID/hackday-repo/$SERVICE_NAME:latest \
            --region $REGION \
            --platform managed \
            --allow-unauthenticated \
            --set-env-vars GOOGLE_CLOUD_PROJECT=$PROJECT_ID

      - name: Get Cloud Run URL
        id: get-url
        run: |
          URL=$(gcloud run services describe $SERVICE_NAME \
            --region $REGION \
            --project $PROJECT_ID \
            --format 'value(status.url)')
          echo "BACKEND_URL=$URL" >> $GITHUB_ENV

      # =============================
      # 2️⃣ FRONTEND (Firebase Hosting)
      # =============================
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Validate Firebase Token
        run: |
<<<<<<< HEAD
          firebase list --token "${{ secrets.FIREBASE_TOKEN }}" || (echo "❌ Token inválido o caducado" && exit 1)

      - name: Disable Firebase Hosting deployment
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          firebase hosting:disable --token ${{ secrets.FIREBASE_TOKEN }} || echo "No hosting to disable"
=======
          echo "🔍 Verificando autenticación con Firebase..."
          firebase list --token "${{ secrets.FIREBASE_TOKEN }}" || (echo "❌ Token inválido o caducado" && exit 1)

      - name: Build Frontend with dynamic API URL
        run: |
          cp $FRONTEND_DIR/.env.example $FRONTEND_DIR/.env
          sed -i '/^REACT_APP_API_URL=/d' $FRONTEND_DIR/.env
          echo "REACT_APP_API_URL=${BACKEND_URL}" >> $FRONTEND_DIR/.env
          cd $FRONTEND_DIR
          npm install
          npm run build

      - name: Deploy Frontend to Firebase Hosting
        working-directory: ${{ env.FRONTEND_DIR }}
        run: firebase deploy --token ${{ secrets.FIREBASE_TOKEN }}
>>>>>>> e43e1194718c748c7abcbace9e38cabb8f78d4da
